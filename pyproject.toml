[project]
name = "cch-003"
version = "0.1.0"
description = "A tool that analyzes and automatically classifies text files."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ollama>=0.6.1",
]
